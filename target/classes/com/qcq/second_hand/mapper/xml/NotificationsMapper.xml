<?xml version="1.0" encoding="UTF-8"?>
<!DOCTYPE mapper PUBLIC "-//mybatis.org//DTD Mapper 3.0//EN" "http://mybatis.org/dtd/mybatis-3-mapper.dtd">
<mapper namespace="com.qcq.second_hand.mapper.NotificationsMapper">

    <select id="findByUserIdOrderByCreateTimeDesc" resultType="com.qcq.second_hand.entity.Notifications">
        SELECT * FROM notifications WHERE user_id = #{userId} ORDER BY create_time DESC
    </select>

    <select id="findByUserIdAndIsRead" resultType="com.qcq.second_hand.entity.Notifications">
        SELECT * FROM notifications WHERE user_id = #{userId} AND is_read = #{isRead}
    </select>

    <select id="countByUserIdAndIsRead" resultType="long">
        SELECT COUNT(*) FROM notifications WHERE user_id = #{userId} AND is_read = #{isRead}
    </select>

    <update id="updateAllUnreadToReadByUserId">
        UPDATE notifications SET is_read = 1 WHERE user_id = #{userId} AND is_read = 0
    </update>

</mapper>
